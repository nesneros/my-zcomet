# File: ~/.config/direnv/direnvrc
# source "$(asdf direnv hook asdf)" 

if [ -f .envrc ] && grep -q "^#--FULL-LOG--$" .envrc ; then
    DIRENV_LOG_FORMAT="%s"
else
    DIRENV_LOG_FORMAT=""
fi

use asdf
